{
  "name": "counter-near-tests",
  "version": "1.0.0",
  "description": "Tests for NEAR counter contract",
  "scripts": {
    "test": "jest",
    "build": "cargo build --target wasm32-unknown-unknown --release",
    "deploy": "ts-node scripts/deploy.ts",
    "dev-deploy": "./scripts/dev-deploy.sh",
    "interact": "ts-node scripts/interact.ts",
    "test:contract": "ts-node scripts/test-contract.ts",
    "generate-wallet": "ts-node scripts/generate-wallet.ts",
    "verify-wallet": "ts-node scripts/verify-wallet.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "jest-util": "^30.0.5",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "near-api-js": "^3.0.0"
  }
}
