{
  "name": "xrpl-cross-chain-htlc",
  "version": "1.0.0",
  "description": "XRPL Cross-Chain HTLC Implementation for EVM ↔ XRPL Swaps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "setup-env": "ts-node scripts/setup-environment.ts",
    "deploy": "ts-node scripts/deploy-xrpl-bridge.ts", 
    "fund-wallets": "ts-node scripts/fund-wallets.ts",
    "fund-check": "ts-node scripts/fund-wallets.ts",
    "integration-test": "ts-node scripts/run-integration-test.ts",
    "clean": "rm -rf dist",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "keywords": [
    "xrpl",
    "cross-chain",
    "htlc",
    "atomic-swaps",
    "blockchain",
    "defi",
    "evm",
    "bridge"
  ],
  "author": "Your Team",
  "license": "MIT",
  "dependencies": {
    "xrpl": "^2.7.0",
    "ethers": "^6.8.1", 
    "dotenv": "^16.3.1",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.8.6",
    "@types/jest": "^29.5.5",
    "@typescript-eslint/eslint-plugin": "^6.7.4",
    "@typescript-eslint/parser": "^6.7.4",
    "eslint": "^8.50.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/xrpl-cross-chain-htlc.git"
  },
  "bugs": {
    "url": "https://github.com/your-org/xrpl-cross-chain-htlc/issues"
  },
  "homepage": "https://github.com/your-org/xrpl-cross-chain-htlc#readme"
}
