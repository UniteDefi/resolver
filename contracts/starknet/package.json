{
  "name": "unite-starknet",
  "version": "1.0.0",
  "description": "StarkNet contracts for Unite DeFi",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "compile": "scarb build",
    "generate:wallet": "ts-node scripts/generate-wallet.ts",
    "deploy:account": "ts-node scripts/deploy-account.ts",
    "setup": "ts-node scripts/simple-deploy.ts",
    "test-connection": "ts-node scripts/test-connection.ts",
    "basic-test": "ts-node scripts/basic-test.ts",
    "deploy-all": "ts-node scripts/deploy-all.ts",
    "check-balance": "ts-node scripts/check-balance.ts",
    "final-deploy": "ts-node scripts/final-deploy.ts",
    "deploy:testnet": "ts-node scripts/deploy.ts",
    "deploy:mainnet": "NETWORK=mainnet ts-node scripts/deploy.ts",
    "deploy:cli": "./scripts/deploy-cli.sh",
    "interact": "ts-node scripts/interact.ts",
    "counter:get": "ts-node scripts/interact.ts get",
    "counter:increase": "ts-node scripts/interact.ts increase",
    "counter:decrease": "ts-node scripts/interact.ts decrease"
  },
  "keywords": [
    "starknet",
    "cairo",
    "defi"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "starknet": "^6.9.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.30",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}
